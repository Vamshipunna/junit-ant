pipeline{
  agent any
  stages{
    stage("clone"){
      steps{
        git clone https://github.com/Vamshipunna/junit-ant.git
      }
    }
    stage("build"){
      steps{
        echo "build"
      }
  }
}
